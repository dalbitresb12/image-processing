import { Grid } from '../components/grid';
import { Layout } from '../components/layout';

const MasksFilters = (): React.ReactElement => {
  return (
    <Layout title="Masks Filters">
      <Grid />
    </Layout>
  );
};

export default MasksFilters;
